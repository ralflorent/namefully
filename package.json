{
  "name": "namefully",
  "version": "1.2.1",
  "description": "Handle personal names in a particular order, way, or shape.",
  "author": "Ralph Florent",
  "license": "MIT",
  "repository": "github:ralflorent/namefully",
  "homepage": "https://namefully.netlify.app",
  "main": "dist/lib/index.js",
  "types": "dist/types/index.d.ts",
  "browser": "dist/umd/namefully.min.js",
  "files": ["dist/**/*"],
  "keywords": ["format", "parse", "human", "personal", "family", "name"],
  "scripts": {
    "example": "webpack --config webpack.dev.js --progress && node ./build/example/index.js",
    "format": "prettier --write '{src,test}/**/*.ts'",
    "prebuild": "shx rm -rf dist build",
    "build": "tsc && tsc -p tsconfig.dts.json && webpack --config webpack.prod.js --progress",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --collectCoverage=true"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "*",
    "clean-webpack-plugin": "^3.0.0",
    "jest": "^29.5.0",
    "prettier": "^3.2.5",
    "shx": "^0.4.0",
    "terser-webpack-plugin": "^2.3.5",
    "ts-jest": "^29.1.0",
    "ts-loader": "^6.2.1",
    "tslint": "^6.0.0",
    "typescript": "~5.3.0",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "printWidth": 120
  },
  "jest": {
    "transform": {
      "^.+.ts$": "ts-jest"
    }
  }
}
